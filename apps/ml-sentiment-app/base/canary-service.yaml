apiVersion: v1
kind: Service
metadata:
  name: ml-sentiment-app-canary
  labels:
    app: ml-sentiment-app
spec:
  selector:
    app: ml-sentiment-app
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP